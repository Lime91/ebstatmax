# Generated by roxygen2: do not edit by hand

export(compute_rejection_rate)
export(exclude_na_blocks)
export(gpc)
export(nparld)
export(perform_test)
export(print_config_to_stderr)
export(print_data_info_to_stderr)
export(read_data)

# manually added exports:

export(CONFIG)

# manually added imports:

import(data.table)  # in order to access data.table special symbols (such as
# ":=", "[]", etc.)
import(dplyr, except=c("last", "first", "between"))  # needed by gpc,
# exceptions to avoid import warnings (conflicts with data.table)
